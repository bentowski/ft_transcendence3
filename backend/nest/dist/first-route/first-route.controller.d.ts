import { FirstRouteService } from './first-route.service';
export declare class FirstRouteController {
    private readonly firstRouteController;
    constructor(firstRouteController: FirstRouteService);
    getHello(): string;
}
